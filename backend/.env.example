DATABASE_URL="replace with your database URL"

JWT_SECRET="replace-with-strong-random-jwt-secret"

JWT_REFRESH_SECRET="replace-with-strong-random-jwt-refresh-secret"
JWT_EMAIL_SECRET="replace-with-strong-random-jwt-email-secret"

NODE_ENV="development"
PORT="5000"

FRONTEND_URL="http://localhost:3000"

# Email provider options (choose one method):
# 1) SendGrid API
SENDGRID_API_KEY=""
# FROM_EMAIL="no-reply@gathrio.com"

# 2) SMTP provider (recommended for local development)
SMTP_HOST="smtp.mailtrap.io"
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
FROM_EMAIL="no-reply@gathrio.local"

# Optional: Ethereal test account (dev-only)
# SMTP_HOST="smtp.ethereal.email"
# SMTP_PORT="587"
# SMTP_USER="your_ethereal_user"
# SMTP_PASS="your_ethereal_pass"
# FROM_EMAIL="no-reply@gathrio.local"

# Redis (optional)
REDIS_URL="redis://localhost:6379"